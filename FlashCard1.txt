// App.jsx
import FlashCard from './components/FlashCard';
import './App.css';
import { useState } from 'react';

function App(){
  const [score, setScore]=useState(0);

  const quizData=[
    {
      id: 1,
      question: "Q. 리액트에서 컴포넌트 이름은 소문자로 시작해도 된다?",
      answer: "❌ 땡! 무조건 대문자로 시작해야 해요"
    },
    {
      id: 2,
      question: "Q. 화면을 그릴 때 여러 태그를 반환하려면 하나의 부모 박스(<>)로 묶어야 한다?",
      answer: "⭕ 정답! 빈 태그(Fragment)로 꼭 묶어주세요"
    },
    {
      id: 3,
      question: "Q. HTML의 class 속성은 리액트에서 무엇으로 써야 할까요?",
      answer: "💡 className으로 써야 합니다"
    },
    {
      id: 4,
      question: "Q. 리액트에서 컴포넌트 이름은 소문자로 시작해도 된다?",
      answer: "❌ 땡! 무조건 대문자로 시작해야 해요"
    },
    {
      id: 5,
      question: "Q. 화면을 그릴 때 여러 태그를 반환하려면 하나의 부모 박스(<>)로 묶어야 한다?",
      answer: "⭕ 정답! 빈 태그(Fragment)로 꼭 묶어주세요"
    },{
      id: 6,
      question: "Q. 리액트 실행 방법은?",
      answer: "💡 npm run dev"
    }
  ];



  const increaseScore=()=>{
    setScore(score+1);
  }

  return(
    <div className='app_wrap'>
      <h2>🧠 리액트 복습 O/X 퀴즈</h2>
      
      <div className="scoreboard">
        현재 점수 : <span>{score}</span> / {quizData.length} 점
      </div>

      <div className="card_grid">
        {/* 배열을 순회하며 카드를 여러 장 찍어냅니다. */}
        {quizData.map((quiz)=>
          <FlashCard
            key={quiz.id}
            data={quiz}
            onCorrect={increaseScore}
          />
        )}
      </div>
    </div>
  )
}

export default App;

// App.css
.app_wrap{
  text-align: center;
  padding: 40px 20px;
  background-color: #f8f9fa;
  min-height: 100vh;
}

.scoreboard{
  background-color: #ffe066;
  padding: 10px 20px;
  border-radius: 30px;
  display: inline-block;
  font-size: 1.2rem;
  font-weight: bold;
  color: #333;
  margin-bottom: 20px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}

.scoreboard span{
  color: #d32f2f;
  font-size: 1.5rem;
}

.card_grid{
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 20px;
  margin-top: 30px;
}

.answer_btn{
  display: flex;
  font-size: 20px;
}

// FlashCard.jsx
import { useState } from "react";
import './FlashCard.css'

function FlashCard({data, onCorrect}){
    const [isFlipped, setIsFlipped]=useState(false);
    const [isScore, setIsScore]=useState(false);

    const flipCard=()=>{
        if(!isFlipped){
            setIsFlipped(true);
        }
    };

    const handleCorrectClick=(e)=>{
        e.stopPropagation();

        if(!isScore){
            onCorrect();
            setIsScore(true);
        }
    };

    return(
        <div
            className={isFlipped ? 'flash_card flipped' : 'flash_card'}
            onClick={flipCard}
        >
            <div className="card_content">
                {isFlipped ? (
                    <div className="answer_wrap">
                        <p className="answer_text">{data.answer}</p>

                        {!isScore ? (
                            <button className="correct_btn" onClick={handleCorrectClick}>
                                맞췄어요! +1
                            </button>
                            
                        ) : (
                            <span className="done_text">채점 완료 👏</span>
                        )}
                    </div>
                ) : (
                    <p className="question_text">{data.question}</p>
                )}
            </div>
        </div>
    )
}

export default FlashCard;

// FlashCard.css
.correct_btn{
    background-color: #4caf50;
    color: white;
    border: none;
    padding: 8px 16px;
    border-radius: 20px;
    font-weight: bold;
    cursor: pointer;
    transition: 0.2s;
}

.correct_btn:hover{
    background-color: #45a049;
    transform: scale(1.05);
}

.done_text{
    color: #888;
    font-size: 0.9rem;
    font-weight: bold;
}

.question_text{
    color: #333;
}

.answer_text{
    color: #2196f3;
}