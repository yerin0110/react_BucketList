// App.jsx
import FlashCard from './components/FlashCard';
import './App.css';
import { useState } from 'react';

function App(){
  const [score, setScore]=useState(0);

  const quizData=[
    {
      id: 1,
      question: "Q. 리액트에서 컴포넌트 이름은 소문자로 시작해도 된다?",
      answer: "무조건 대문자로 시작해야 해요",
      correctAnswer: false
    },
    {
      id: 2,
      question: "Q. 화면을 그릴 때 여러 태그를 반환하려면 하나의 부모 박스(<>)로 묶어야 한다?",
      answer: "반드시 부모 태그나 빈 태그(Fragment)로 꼭 묶어주세요",
      correctAnswer: true
    },
    {
      id: 3,
      question: "Q. HTML의 class 속성은 class로 써도 무방하다?",
      answer: "className으로 써야 합니다",
      correctAnswer: false
    },
    {
      id: 4,
      question: "Q. 리액트에서 컴포넌트 이름은 소문자로 시작해도 된다?",
      answer: "무조건 대문자로 시작해야 해요",
      correctAnswer: false
    },
    {
      id: 5,
      question: "Q. 화면을 그릴 때 여러 태그를 반환하려면 하나의 부모 박스(<>)로 묶어야 한다?",
      answer: "빈 태그(Fragment)로 꼭 묶어주세요",
      correctAnswer: true
    },{
      id: 6,
      question: "Q. HTML의 class 속성은 class로 써도 무방하다?",
      answer: "className으로 써야 합니다",
      correctAnswer: false
    }
  ];

  const increaseScore=()=>{
    setScore(score+1);
  }

  return(
    <div className='app_wrap'>
      <h2>🧠 리액트 복습 O/X 퀴즈</h2>
      
      <div className="scoreboard">
        현재 점수 : <span>{score}</span> / {quizData.length} 점
      </div>

      <div className="card_grid">
        {/* 배열을 순회하며 카드를 여러 장 찍어냅니다. */}
        {quizData.map((quiz)=>
          <FlashCard
            key={quiz.id}
            data={quiz}
            onCorrect={increaseScore}
          />
        )}
      </div>
    </div>
  )
}

export default App;

// App.css
.app_wrap{
  text-align: center;
  padding: 40px 20px;
  background-color: #f8f9fa;
  min-height: 100vh;
}

.scoreboard{
  background-color: #ffe066;
  padding: 10px 20px;
  border-radius: 30px;
  display: inline-block;
  font-size: 1.2rem;
  font-weight: bold;
  color: #333;
  margin-bottom: 20px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}

.scoreboard span{
  color: #d32f2f;
  font-size: 1.5rem;
}

.card_grid{
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 20px;
  margin-top: 30px;
}

.answer_btn{
  display: flex;
  font-size: 20px;
}